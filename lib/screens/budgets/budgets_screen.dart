import 'package:flutter/material.dart';

class budgets_screen extends StatelessWidget {
  const budgets_screen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Budgets"),
    );
  }
}